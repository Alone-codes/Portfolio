for i in {1..10}
do
  DATE=$(date -d "2025-10-$i $((RANDOM%12+1)):00:00" +"%Y-%m-%d %H:%M:%S")
  
  echo "real update $i" >> file.txt
  git add .
  
  GIT_AUTHOR_DATE="$DATE" GIT_COMMITTER_DATE="$DATE" \
  git commit -m "real update $i"
done